'use client';

import styled from 'styled-components';

const RewardsPage = () => {
	return (
		<Wrapper>
			<h1>Rewards</h1>
		</Wrapper>
	);
};

const Wrapper = styled.div`
	margin-top: 200px;
`;

export default RewardsPage;
